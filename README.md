Vagrant Start Scripts
=====================

Configuration
-------------
To create the initial configuration copy `config-example.bat` and name the
copy `config.bat`.

The batch files will load from `config-example.bat` if `config.bat` has not
been found.